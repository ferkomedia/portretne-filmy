---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout title="Admin | Správa" noindex={true}>
  <section class="heroSection">
    <div class="heroContent">
      <h1>Správa FerkoMedia</h1>
      <p class="heroLead">Faktúry, workshopy, email marketing, social media</p>
    </div>
  </section>

  <!-- TABS -->
  <section class="section">
    <div class="adminTabs">
      <button class="adminTab active" data-tab="invoices">Faktúry</button>
      <button class="adminTab" data-tab="workshops">Workshopy</button>
      <button class="adminTab" data-tab="email">Email Marketing</button>
      <button class="adminTab" data-tab="social">Social Media</button>
    </div>

    <!-- FAKTÚRY TAB -->
    <div class="tabContent active" id="tab-invoices">
      <div class="statsGrid">
        <div class="statCard">
          <span class="statValue" id="statTotal">-</span>
          <span class="statLabel">Faktúr celkom</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="statRevenue">-</span>
          <span class="statLabel">Celkový príjem</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="statMonthCount">-</span>
          <span class="statLabel">Tento mesiac</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="statMonthRevenue">-</span>
          <span class="statLabel">Príjem tento mesiac</span>
        </div>
      </div>

      <div class="invoiceControls">
        <input type="text" id="searchInvoice" placeholder="Hľadať podľa mena, emailu, IČO..." class="searchInput" />
        <button class="btn btn--primary" id="loadInvoices">Načítať faktúry</button>
      </div>

      <div class="tableWrapper">
        <table class="invoiceTable">
          <thead>
            <tr>
              <th>Číslo</th>
              <th>Dátum</th>
              <th>Zákazník</th>
              <th>Firma / IČO</th>
              <th>Produkt</th>
              <th>Suma</th>
              <th>Stav</th>
            </tr>
          </thead>
          <tbody id="invoiceTableBody">
            <tr>
              <td colspan="7" class="emptyState">Klikni na "Načítať faktúry" pre zobrazenie</td>
            </tr>
          </tbody>
        </table>
      </div>

      <div id="loadMoreWrapper" style="display:none; text-align:center; margin-top:1rem;">
        <button class="btn btn--ghost" id="loadMore">Načítať ďalšie</button>
      </div>
    </div>

    <!-- WORKSHOPY TAB -->
    <div class="tabContent" id="tab-workshops">
      <h2>Pridať nový termín</h2>
      <div class="formCard">
        <form id="addTermForm">
          <div class="formGrid">
            <div class="field">
              <label for="title">Typ workshopu</label>
              <select id="title" required>
                <option value="">Vyber typ</option>
                <option value="Web za 1 deň">Web za 1 deň</option>
                <option value="E-shop za 1 deň">E-shop za 1 deň</option>
                <option value="Google reklama za 1 deň">Google reklama za 1 deň</option>
                <option value="Sociálne siete za 1 deň">Sociálne siete za 1 deň</option>
                <option value="AI nástroje za 1 deň">AI nástroje za 1 deň</option>
              </select>
            </div>

            <div class="field">
              <label for="date">Dátum</label>
              <input id="date" type="text" placeholder="DD.MM.RRRR" required />
            </div>

            <div class="field">
              <label for="city">Mesto</label>
              <input id="city" type="text" value="Martin" required />
            </div>

            <div class="field">
              <label for="price">Cena (€)</label>
              <input id="price" type="number" value="149" required />
            </div>

            <div class="field">
              <label for="capacity">Kapacita</label>
              <input id="capacity" type="number" value="12" required />
            </div>

            <div class="field">
              <label for="status">Status</label>
              <select id="status">
                <option value="open">Otvorený</option>
                <option value="closed">Zatvorený</option>
              </select>
            </div>
          </div>

          <div class="formActions">
            <button type="submit" class="btn btn--primary">Generovať JSON</button>
          </div>
        </form>

        <div id="jsonOutput" style="display:none; margin-top:1.5rem;">
          <h3>Nový JSON záznam:</h3>
          <pre id="jsonCode"></pre>
          <p class="help">Skopíruj tento záznam a pridaj ho do súboru src/data/workshop-terms.json</p>
        </div>
      </div>

      <h2 style="margin-top:3rem;">Ako upraviť termíny</h2>
      <div class="stepsGrid">
        <div class="stepCard">
          <span class="stepNum">1</span>
          <h4>Otvor súbor</h4>
          <p>src/data/workshop-terms.json</p>
        </div>
        <div class="stepCard">
          <span class="stepNum">2</span>
          <h4>Uprav JSON</h4>
          <p>Pridaj, zmeň alebo odstráň termíny.</p>
        </div>
        <div class="stepCard">
          <span class="stepNum">3</span>
          <h4>Commit a Push</h4>
          <p>git add . && git commit && git push</p>
        </div>
        <div class="stepCard">
          <span class="stepNum">4</span>
          <h4>Hotovo</h4>
          <p>Cloudflare automaticky deployuje.</p>
        </div>
      </div>
    </div>

    <!-- EMAIL MARKETING TAB -->
    <div class="tabContent" id="tab-email">
      <div class="statsGrid">
        <div class="statCard">
          <span class="statValue" id="emailStatTotal">-</span>
          <span class="statLabel">Kontaktov celkom</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="emailStatWorkshops">-</span>
          <span class="statLabel">Z workshopov</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="emailStatContact">-</span>
          <span class="statLabel">Z kontaktu</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="emailStatCampaigns">-</span>
          <span class="statLabel">Odoslaných kampaní</span>
        </div>
      </div>

      <div class="subTabs">
        <button class="subTab active" data-subtab="contacts">Kontakty</button>
        <button class="subTab" data-subtab="campaigns">Kampane</button>
        <button class="subTab" data-subtab="templates">Šablóny</button>
      </div>

      <div class="subTabContent active" id="subtab-contacts">
        <div class="emailControls">
          <input type="text" id="searchContacts" placeholder="Hľadať kontakty..." class="searchInput" />
          <button class="btn btn--primary" id="loadContacts">Načítať kontakty</button>
          <button class="btn btn--ghost" id="exportContacts">Export CSV</button>
        </div>

        <div class="tableWrapper">
          <table class="invoiceTable">
            <thead>
              <tr>
                <th><input type="checkbox" id="selectAll" /></th>
                <th>Meno</th>
                <th>Email</th>
                <th>Zdroj</th>
                <th>Dátum</th>
                <th>Akcie</th>
              </tr>
            </thead>
            <tbody id="contactsTableBody">
              <tr>
                <td colspan="6" class="emptyState">Klikni na "Načítať kontakty"</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div class="subTabContent" id="subtab-campaigns">
        <button class="btn btn--primary" id="createCampaignBtn" style="margin-bottom:2rem;">+ Vytvoriť novú kampaň</button>
        <p class="help">Email kampane - funkcionalita v príprave</p>
      </div>

      <div class="subTabContent" id="subtab-templates">
        <h3>Email šablóny</h3>
        <p class="help">Šablóny - funkcionalita v príprave</p>
      </div>
    </div>

    <!-- SOCIAL MEDIA TAB -->
    <div class="tabContent" id="tab-social">
      <!-- Social Stats -->
      <div class="statsGrid">
        <div class="statCard">
          <span class="statValue" id="socialStatPosts">24</span>
          <span class="statLabel">Naplánovaných príspevkov</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="socialStatDrafts">8</span>
          <span class="statLabel">Konceptov</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="socialStatPublished">156</span>
          <span class="statLabel">Publikovaných tento mesiac</span>
        </div>
        <div class="statCard">
          <span class="statValue" id="socialStatEngagement">12.4k</span>
          <span class="statLabel">Celkový engagement</span>
        </div>
      </div>

      <!-- Social Sub-tabs -->
      <div class="subTabs">
        <button class="subTab active" data-subtab="social-composer">Composer</button>
        <button class="subTab" data-subtab="social-calendar">Kalendár</button>
        <button class="subTab" data-subtab="social-queue">Fronta</button>
        <button class="subTab" data-subtab="social-analytics">Štatistiky</button>
        <button class="subTab" data-subtab="social-media">Média</button>
        <button class="subTab" data-subtab="social-accounts">Účty</button>
      </div>

      <!-- COMPOSER SUBTAB -->
      <div class="subTabContent active" id="subtab-social-composer">
        <div class="composerLayout">
          <!-- Left: Composer -->
          <div class="composerPanel">
            <h3>Vytvoriť príspevok</h3>

            <!-- Platform Selection -->
            <div class="platformSelector">
              <label class="platformBtn" data-platform="facebook">
                <input type="checkbox" name="platforms" value="facebook" checked />
                <span class="platformIcon">📘</span>
                <span>Facebook</span>
              </label>
              <label class="platformBtn" data-platform="instagram">
                <input type="checkbox" name="platforms" value="instagram" checked />
                <span class="platformIcon">📷</span>
                <span>Instagram</span>
              </label>
              <label class="platformBtn" data-platform="linkedin">
                <input type="checkbox" name="platforms" value="linkedin" />
                <span class="platformIcon">💼</span>
                <span>LinkedIn</span>
              </label>
              <label class="platformBtn" data-platform="twitter">
                <input type="checkbox" name="platforms" value="twitter" />
                <span class="platformIcon">🐦</span>
                <span>Twitter/X</span>
              </label>
            </div>

            <!-- Text Editor -->
            <div class="field fieldFull">
              <label>Text príspevku</label>
              <textarea id="postContent" rows="8" placeholder="Napíš svoj príspevok..."></textarea>
              <div class="textStats">
                <span id="charCount">0</span> / <span id="charLimit">2200</span> znakov
              </div>
            </div>

            <!-- Media Upload -->
            <div class="field fieldFull">
              <label>Obrázky / Video</label>
              <div class="mediaUploadZone" id="mediaUploadZone">
                <div class="uploadPlaceholder">
                  <span class="uploadIcon">📁</span>
                  <p>Klikni alebo presuň súbory sem</p>
                  <input type="file" id="mediaUpload" accept="image/*,video/*" multiple hidden />
                </div>
                <div class="uploadedMedia" id="uploadedMedia"></div>
              </div>
            </div>

            <!-- Hashtag Suggestions -->
            <div class="field fieldFull">
              <label>Hashtag návrhy</label>
              <div class="hashtagSuggestions">
                <button class="hashtagTag" onclick="addHashtag('#portret')">#portret</button>
                <button class="hashtagTag" onclick="addHashtag('#video')">#video</button>
                <button class="hashtagTag" onclick="addHashtag('#marketing')">#marketing</button>
                <button class="hashtagTag" onclick="addHashtag('#business')">#business</button>
                <button class="hashtagTag" onclick="addHashtag('#slovensko')">#slovensko</button>
                <button class="btn btn--ghost btn--small" onclick="generateHashtags()">🤖 AI návrhy</button>
              </div>
            </div>

            <!-- Scheduling -->
            <div class="schedulingPanel">
              <div class="field">
                <label>
                  <input type="radio" name="scheduleType" value="now" checked />
                  Publikovať teraz
                </label>
              </div>
              <div class="field">
                <label>
                  <input type="radio" name="scheduleType" value="schedule" />
                  Naplánovať
                </label>
                <input type="datetime-local" id="scheduleDateTime" disabled />
              </div>
              <div class="field">
                <label>
                  <input type="radio" name="scheduleType" value="queue" />
                  Pridať do fronty
                </label>
              </div>
              <div class="field">
                <label>
                  <input type="radio" name="scheduleType" value="draft" />
                  Uložiť ako koncept
                </label>
              </div>
            </div>

            <!-- Actions -->
            <div class="composerActions">
              <button class="btn btn--primary" id="publishPostBtn">📤 Publikovať</button>
              <button class="btn btn--ghost" id="saveDraftBtn">💾 Uložiť koncept</button>
              <button class="btn btn--ghost" id="clearComposerBtn">🗑️ Vymazať</button>
            </div>
          </div>

          <!-- Right: Preview -->
          <div class="previewPanel">
            <h3>Náhľad</h3>
            <div class="previewTabs">
              <button class="previewTab active" data-preview="facebook">Facebook</button>
              <button class="previewTab" data-preview="instagram">Instagram</button>
              <button class="previewTab" data-preview="linkedin">LinkedIn</button>
              <button class="previewTab" data-preview="twitter">Twitter</button>
            </div>

            <!-- Facebook Preview -->
            <div class="previewContent active" id="preview-facebook">
              <div class="socialPreview facebookPreview">
                <div class="previewHeader">
                  <div class="previewAvatar">FM</div>
                  <div class="previewMeta">
                    <strong>FerkoMedia</strong>
                    <span class="previewTime">Práve teraz</span>
                  </div>
                </div>
                <div class="previewText" id="fbPreviewText">Tvoj text sa zobrazí tu...</div>
                <div class="previewMedia" id="fbPreviewMedia"></div>
                <div class="previewStats">
                  <span>👍 Páči sa mi to</span>
                  <span>💬 Komentovať</span>
                  <span>↗️ Zdieľať</span>
                </div>
              </div>
            </div>

            <!-- Instagram Preview -->
            <div class="previewContent" id="preview-instagram">
              <div class="socialPreview instagramPreview">
                <div class="previewHeader">
                  <div class="previewAvatar">FM</div>
                  <div class="previewMeta">
                    <strong>ferkomedia</strong>
                  </div>
                </div>
                <div class="previewMedia" id="igPreviewMedia"></div>
                <div class="previewStats">
                  <span>❤️ Páči sa mi to</span>
                  <span>💬 Komentovať</span>
                  <span>📤 Zdieľať</span>
                </div>
                <div class="previewText" id="igPreviewText">Tvoj text sa zobrazí tu...</div>
              </div>
            </div>

            <!-- LinkedIn Preview -->
            <div class="previewContent" id="preview-linkedin">
              <div class="socialPreview linkedinPreview">
                <div class="previewHeader">
                  <div class="previewAvatar">FM</div>
                  <div class="previewMeta">
                    <strong>FerkoMedia</strong>
                    <span class="previewTime">Práve teraz</span>
                  </div>
                </div>
                <div class="previewText" id="liPreviewText">Tvoj text sa zobrazí tu...</div>
                <div class="previewMedia" id="liPreviewMedia"></div>
                <div class="previewStats">
                  <span>👍 Súhlas</span>
                  <span>💬 Komentovať</span>
                  <span>↗️ Zdieľať</span>
                </div>
              </div>
            </div>

            <!-- Twitter Preview -->
            <div class="previewContent" id="preview-twitter">
              <div class="socialPreview twitterPreview">
                <div class="previewHeader">
                  <div class="previewAvatar">FM</div>
                  <div class="previewMeta">
                    <strong>FerkoMedia</strong>
                    <span class="previewHandle">@ferkomedia</span>
                  </div>
                </div>
                <div class="previewText" id="twPreviewText">Tvoj text sa zobrazí tu...</div>
                <div class="previewMedia" id="twPreviewMedia"></div>
                <div class="previewStats">
                  <span>💬 Odpovedať</span>
                  <span>🔄 Retweetovať</span>
                  <span>❤️ Páči sa mi to</span>
                </div>
              </div>
            </div>

            <!-- Best Time Suggestion -->
            <div class="bestTimeCard">
              <h4>⏰ Najlepší čas na publikovanie</h4>
              <p>Dnes: <strong>18:00 - 20:00</strong></p>
              <p>Zajtra: <strong>12:00 - 14:00</strong></p>
            </div>
          </div>
        </div>
      </div>

      <!-- CALENDAR SUBTAB -->
      <div class="subTabContent" id="subtab-social-calendar">
        <div class="calendarHeader">
          <h3>Kalendár príspevkov</h3>
          <div class="calendarControls">
            <button class="btn btn--ghost" onclick="prevMonth()">← Predošlý</button>
            <span id="currentMonth">Január 2026</span>
            <button class="btn btn--ghost" onclick="nextMonth()">Ďalší →</button>
          </div>
        </div>

        <div class="calendar">
          <div class="calendarGrid">
            <div class="calendarDay dayHeader">Po</div>
            <div class="calendarDay dayHeader">Ut</div>
            <div class="calendarDay dayHeader">St</div>
            <div class="calendarDay dayHeader">Št</div>
            <div class="calendarDay dayHeader">Pi</div>
            <div class="calendarDay dayHeader">So</div>
            <div class="calendarDay dayHeader">Ne</div>

            <!-- Days will be generated by JS -->
            <div class="calendarDay">13</div>
            <div class="calendarDay">14</div>
            <div class="calendarDay today">
              15
              <div class="calendarPosts">
                <div class="calendarPost fb">📘 10:00</div>
                <div class="calendarPost ig">📷 14:00</div>
              </div>
            </div>
            <div class="calendarDay">16</div>
            <div class="calendarDay">17</div>
            <div class="calendarDay">18</div>
            <div class="calendarDay">19</div>
          </div>
        </div>

        <div class="scheduledPosts">
          <h4>Naplánované príspevky na dnes</h4>
          <div class="postsList">
            <div class="postItem">
              <div class="postPlatforms">
                <span class="platformBadge fb">📘</span>
                <span class="platformBadge ig">📷</span>
              </div>
              <div class="postContent">
                <strong>10:00</strong> - Nové workshopy pre rok 2026...
              </div>
              <div class="postActions">
                <button class="btn btn--ghost btn--small">✏️ Upraviť</button>
                <button class="btn btn--ghost btn--small">🗑️ Vymazať</button>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- QUEUE SUBTAB -->
      <div class="subTabContent" id="subtab-social-queue">
        <h3>Fronta príspevkov</h3>
        <p class="help">Automatické publikovanie podľa nastaveného rozvrhu</p>

        <div class="queueSettings">
          <h4>Nastavenie fronty</h4>
          <div class="formGrid">
            <div class="field">
              <label>Publikovať v pondelok</label>
              <input type="time" value="09:00" />
              <input type="time" value="17:00" />
            </div>
            <div class="field">
              <label>Publikovať v utorok</label>
              <input type="time" value="09:00" />
              <input type="time" value="17:00" />
            </div>
          </div>
          <button class="btn btn--primary">💾 Uložiť nastavenia</button>
        </div>

        <div class="queueList">
          <h4>Príspevky vo fronte (8)</h4>
          <div class="postsList">
            <div class="postItem draggable">
              <span class="dragHandle">⋮⋮</span>
              <div class="postPlatforms">
                <span class="platformBadge fb">📘</span>
              </div>
              <div class="postContent">
                <strong>1.</strong> Workshop o AI nástrojoch začína už budúci týždeň...
              </div>
              <div class="postActions">
                <button class="btn btn--ghost btn--small">✏️</button>
                <button class="btn btn--ghost btn--small">🗑️</button>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- ANALYTICS SUBTAB -->
      <div class="subTabContent" id="subtab-social-analytics">
        <h3>Štatistiky a Analytics</h3>

        <div class="analyticsDateRange">
          <select>
            <option>Posledných 7 dní</option>
            <option>Posledných 30 dní</option>
            <option>Tento mesiac</option>
            <option>Vlastné obdobie</option>
          </select>
        </div>

        <div class="statsGrid">
          <div class="statCard">
            <span class="statValue">45.2k</span>
            <span class="statLabel">Celkový dosah</span>
            <span class="statChange positive">+12%</span>
          </div>
          <div class="statCard">
            <span class="statValue">3.8k</span>
            <span class="statLabel">Engagement</span>
            <span class="statChange positive">+8%</span>
          </div>
          <div class="statCard">
            <span class="statValue">892</span>
            <span class="statLabel">Nových followerov</span>
            <span class="statChange positive">+15%</span>
          </div>
          <div class="statCard">
            <span class="statValue">4.2%</span>
            <span class="statLabel">Engagement Rate</span>
            <span class="statChange negative">-2%</span>
          </div>
        </div>

        <div class="analyticsChart">
          <h4>Engagement v čase</h4>
          <div class="chartPlaceholder">
            <p>📊 Graf engagement-u (integrácia s Chart.js)</p>
          </div>
        </div>

        <div class="topPosts">
          <h4>Top príspevky tento mesiac</h4>
          <div class="postsList">
            <div class="postItem">
              <div class="postPlatforms">
                <span class="platformBadge ig">📷</span>
              </div>
              <div class="postContent">
                <strong>15.1.2026</strong> - Before & After portrétu...
                <div class="postStats">
                  ❤️ 234 · 💬 45 · 📤 12
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- MEDIA LIBRARY SUBTAB -->
      <div class="subTabContent" id="subtab-social-media">
        <h3>Knižnica médií</h3>
        <button class="btn btn--primary" style="margin-bottom:1rem;">📤 Nahrať súbory</button>

        <div class="mediaFilters">
          <select>
            <option>Všetky typy</option>
            <option>Obrázky</option>
            <option>Videá</option>
          </select>
          <select>
            <option>Všetky kategórie</option>
            <option>Workshopy</option>
            <option>Portréty</option>
            <option>Eventy</option>
          </select>
        </div>

        <div class="mediaGrid">
          <div class="mediaItem">
            <div class="mediaThumb" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);"></div>
            <div class="mediaInfo">
              <span class="mediaName">workshop-promo.jpg</span>
              <span class="mediaDate">14.1.2026</span>
            </div>
          </div>
          <div class="mediaItem">
            <div class="mediaThumb" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);"></div>
            <div class="mediaInfo">
              <span class="mediaName">portret-cliente.jpg</span>
              <span class="mediaDate">12.1.2026</span>
            </div>
          </div>
          <div class="mediaItem">
            <div class="mediaThumb" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);"></div>
            <div class="mediaInfo">
              <span class="mediaName">office-team.jpg</span>
              <span class="mediaDate">10.1.2026</span>
            </div>
          </div>
        </div>
      </div>

      <!-- ACCOUNTS SUBTAB -->
      <div class="subTabContent" id="subtab-social-accounts">
        <h3>Pripojené účty</h3>
        <button class="btn btn--primary" style="margin-bottom:2rem;">➕ Pripojiť nový účet</button>

        <div class="accountsList">
          <div class="accountCard connected">
            <div class="accountHeader">
              <span class="platformIcon large">📘</span>
              <div class="accountInfo">
                <h4>Facebook Page</h4>
                <p>FerkoMedia - Portrétne filmy</p>
              </div>
              <span class="statusBadge active">Aktívny</span>
            </div>
            <div class="accountStats">
              <div><strong>12.4k</strong> Páči sa mi to</div>
              <div><strong>11.8k</strong> Sledujúcich</div>
            </div>
            <div class="accountActions">
              <button class="btn btn--ghost btn--small">⚙️ Nastavenia</button>
              <button class="btn btn--ghost btn--small">🔌 Odpojiť</button>
            </div>
          </div>

          <div class="accountCard connected">
            <div class="accountHeader">
              <span class="platformIcon large">📷</span>
              <div class="accountInfo">
                <h4>Instagram Business</h4>
                <p>@ferkomedia</p>
              </div>
              <span class="statusBadge active">Aktívny</span>
            </div>
            <div class="accountStats">
              <div><strong>8.9k</strong> Sledujúcich</div>
              <div><strong>432</strong> Príspevkov</div>
            </div>
            <div class="accountActions">
              <button class="btn btn--ghost btn--small">⚙️ Nastavenia</button>
              <button class="btn btn--ghost btn--small">🔌 Odpojiť</button>
            </div>
          </div>

          <div class="accountCard">
            <div class="accountHeader">
              <span class="platformIcon large">💼</span>
              <div class="accountInfo">
                <h4>LinkedIn Page</h4>
                <p>FerkoMedia</p>
              </div>
              <span class="statusBadge inactive">Nepripojený</span>
            </div>
            <div class="accountActions">
              <button class="btn btn--primary btn--small">🔗 Pripojiť účet</button>
            </div>
          </div>

          <div class="accountCard">
            <div class="accountHeader">
              <span class="platformIcon large">🐦</span>
              <div class="accountInfo">
                <h4>Twitter / X</h4>
                <p>@ferkomedia</p>
              </div>
              <span class="statusBadge inactive">Nepripojený</span>
            </div>
            <div class="accountActions">
              <button class="btn btn--primary btn--small">🔗 Pripojiť účet</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <script>
    const adminPassword = "MyFilmy2026@";

    // Tab switching
    document.querySelectorAll('.adminTab').forEach(tab => {
      tab.addEventListener('click', function() {
        document.querySelectorAll('.adminTab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.tabContent').forEach(c => c.classList.remove('active'));

        this.classList.add('active');
        document.getElementById('tab-' + this.dataset.tab).classList.add('active');

        if (this.dataset.tab === 'email') {
          loadEmailStats();
        }
      });
    });

    // Sub-tab switching
    document.querySelectorAll('.subTab').forEach(tab => {
      tab.addEventListener('click', function() {
        document.querySelectorAll('.subTab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.subTabContent').forEach(c => c.classList.remove('active'));

        this.classList.add('active');
        document.getElementById('subtab-' + this.dataset.subtab).classList.add('active');
      });
    });

    // Workshop JSON generator
    document.getElementById('addTermForm').addEventListener('submit', function(e) {
      e.preventDefault();
      var term = {
        title: document.getElementById('title').value,
        date: document.getElementById('date').value,
        city: document.getElementById('city').value,
        price: parseInt(document.getElementById('price').value),
        capacity: parseInt(document.getElementById('capacity').value),
        status: document.getElementById('status').value
      };
      document.getElementById('jsonCode').textContent = JSON.stringify(term, null, 2);
      document.getElementById('jsonOutput').style.display = 'block';
    });

    // Invoice loading (simplified)
    let currentOffset = 0;
    const limit = 50;
    let allInvoices = [];

    document.getElementById('loadInvoices').addEventListener('click', loadInvoices);

    async function loadInvoices(append = false) {
      const btn = document.getElementById('loadInvoices');
      btn.disabled = true;
      btn.textContent = 'Načítavam...';

      try {
        const apiKey = prompt('Zadajte admin kľúč:', 'fk2026-admin');
        if (!apiKey) {
          btn.disabled = false;
          btn.textContent = 'Načítať faktúry';
          return;
        }

        const response = await fetch(`/api/get-invoices?key=${encodeURIComponent(apiKey)}&limit=${limit}&offset=${currentOffset}`);
        const data = await response.json();

        if (data.error) {
          alert('Chyba: ' + data.error);
          btn.disabled = false;
          btn.textContent = 'Načítať faktúry';
          return;
        }

        if (data.stats) {
          document.getElementById('statTotal').textContent = data.stats.totalInvoices || 0;
          document.getElementById('statRevenue').textContent = (data.stats.totalRevenue || 0).toLocaleString('sk-SK') + ' €';
          document.getElementById('statMonthCount').textContent = data.stats.monthlyCount || 0;
          document.getElementById('statMonthRevenue').textContent = (data.stats.monthlyRevenue || 0).toLocaleString('sk-SK') + ' €';
        }

        allInvoices = data.invoices || [];
        renderInvoices(allInvoices);

      } catch (err) {
        alert('Chyba pri načítaní: ' + err.message);
      }

      btn.disabled = false;
      btn.textContent = 'Načítať faktúry';
    }

    function renderInvoices(invoices) {
      const tbody = document.getElementById('invoiceTableBody');

      if (!invoices || invoices.length === 0) {
        tbody.innerHTML = '<tr><td colspan="7" class="emptyState">Žiadne faktúry</td></tr>';
        return;
      }

      tbody.innerHTML = invoices.map(inv => `
        <tr>
          <td><strong>${inv.id}</strong></td>
          <td>${new Date(inv.createdAt).toLocaleDateString('sk-SK')}</td>
          <td>
            <div>${inv.customerName}</div>
            <div class="subtext">${inv.customerEmail}</div>
          </td>
          <td>
            ${inv.companyName ? `<div>${inv.companyName}</div><div class="subtext">IČO: ${inv.companyIco}</div>` : '<span class="subtext">FO</span>'}
          </td>
          <td>${inv.productName}</td>
          <td class="amount">${inv.amount.toLocaleString('sk-SK', { minimumFractionDigits: 2 })} €</td>
          <td><span class="badge badge--success">Uhradená</span></td>
        </tr>
      `).join('');
    }

    // Email Marketing (simplified)
    async function loadEmailStats() {
      try {
        const response = await fetch('/api/contacts/stats', {
          headers: { 'Authorization': `Bearer ${adminPassword}` }
        });
        const data = await response.json();

        if (data.ok) {
          document.getElementById('emailStatTotal').textContent = data.total || 0;
          document.getElementById('emailStatWorkshops').textContent = data.workshop || 0;
          document.getElementById('emailStatContact').textContent = data.kontakt || 0;
          document.getElementById('emailStatCampaigns').textContent = data.campaigns || 0;
        }
      } catch (err) {
        console.error('Error loading email stats:', err);
      }
    }

    document.getElementById('loadContacts')?.addEventListener('click', async function() {
      const btn = this;
      btn.disabled = true;
      btn.textContent = 'Načítavam...';

      try {
        const response = await fetch('/api/contacts/list', {
          headers: { 'Authorization': `Bearer ${adminPassword}` }
        });
        const data = await response.json();

        if (data.contacts) {
          const tbody = document.getElementById('contactsTableBody');
          tbody.innerHTML = data.contacts.map(c => `
            <tr>
              <td><input type="checkbox" /></td>
              <td>${c.name || '-'}</td>
              <td>${c.email}</td>
              <td>${c.source}</td>
              <td>${new Date(c.createdAt).toLocaleDateString('sk-SK')}</td>
              <td><button class="btn btn--ghost btn--small">Vymazať</button></td>
            </tr>
          `).join('');
        }
      } catch (err) {
        console.error(err);
      }

      btn.disabled = false;
      btn.textContent = 'Načítať kontakty';
    });

    // SOCIAL MEDIA FUNCTIONALITY

    // Real-time preview update
    const postContent = document.getElementById('postContent');
    if (postContent) {
      postContent.addEventListener('input', updatePreviews);
    }

    function updatePreviews() {
      const text = postContent.value || 'Tvoj text sa zobrazí tu...';
      document.getElementById('fbPreviewText').textContent = text;
      document.getElementById('igPreviewText').textContent = text;
      document.getElementById('liPreviewText').textContent = text;
      document.getElementById('twPreviewText').textContent = text;

      const charCount = text.length;
      document.getElementById('charCount').textContent = charCount;
    }

    // Preview tab switching
    document.querySelectorAll('.previewTab').forEach(tab => {
      tab.addEventListener('click', function() {
        document.querySelectorAll('.previewTab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.previewContent').forEach(c => c.classList.remove('active'));

        this.classList.add('active');
        document.getElementById('preview-' + this.dataset.preview).classList.add('active');
      });
    });

    // Platform selection
    document.querySelectorAll('.platformBtn input').forEach(checkbox => {
      checkbox.addEventListener('change', function() {
        this.closest('.platformBtn').classList.toggle('selected', this.checked);
      });
    });

    // Schedule type
    document.querySelectorAll('input[name="scheduleType"]').forEach(radio => {
      radio.addEventListener('change', function() {
        const datetimeInput = document.getElementById('scheduleDateTime');
        datetimeInput.disabled = this.value !== 'schedule';
      });
    });

    // Media upload
    const uploadZone = document.getElementById('mediaUploadZone');
    const mediaUpload = document.getElementById('mediaUpload');

    if (uploadZone && mediaUpload) {
      uploadZone.addEventListener('click', () => mediaUpload.click());

      mediaUpload.addEventListener('change', function(e) {
        const files = Array.from(e.target.files);
        const uploadedMedia = document.getElementById('uploadedMedia');

        files.forEach(file => {
          const reader = new FileReader();
          reader.onload = function(event) {
            const mediaItem = document.createElement('div');
            mediaItem.className = 'mediaPreviewItem';
            mediaItem.innerHTML = `
              <img src="${event.target.result}" alt="${file.name}" />
              <button class="removeMedia" onclick="this.parentElement.remove()">×</button>
            `;
            uploadedMedia.appendChild(mediaItem);

            // Update all previews
            updateMediaPreviews();
          };
          reader.readAsDataURL(file);
        });
      });
    }

    function updateMediaPreviews() {
      const mediaItems = document.querySelectorAll('.mediaPreviewItem img');
      const mediaSrc = mediaItems.length > 0 ? mediaItems[0].src : '';

      ['fb', 'ig', 'li', 'tw'].forEach(platform => {
        const mediaPreview = document.getElementById(`${platform}PreviewMedia`);
        if (mediaSrc) {
          mediaPreview.innerHTML = `<img src="${mediaSrc}" alt="Preview" style="width:100%; border-radius:8px;" />`;
        } else {
          mediaPreview.innerHTML = '';
        }
      });
    }

    // Publish post
    document.getElementById('publishPostBtn')?.addEventListener('click', function() {
      const selectedPlatforms = Array.from(document.querySelectorAll('.platformBtn input:checked')).map(cb => cb.value);
      const text = postContent.value;
      const scheduleType = document.querySelector('input[name="scheduleType"]:checked').value;

      if (selectedPlatforms.length === 0) {
        alert('Vyber aspoň jednu platformu!');
        return;
      }

      if (!text.trim()) {
        alert('Napíš text príspevku!');
        return;
      }

      const message = scheduleType === 'now'
        ? `Publikujem na: ${selectedPlatforms.join(', ')}`
        : scheduleType === 'schedule'
        ? `Naplánovane na: ${selectedPlatforms.join(', ')}`
        : scheduleType === 'queue'
        ? `Pridané do fronty: ${selectedPlatforms.join(', ')}`
        : `Uložené ako koncept`;

      alert(message);

      // Clear form
      if (scheduleType !== 'draft') {
        document.getElementById('clearComposerBtn').click();
      }
    });

    // Save draft
    document.getElementById('saveDraftBtn')?.addEventListener('click', function() {
      alert('Koncept uložený!');
    });

    // Clear composer
    document.getElementById('clearComposerBtn')?.addEventListener('click', function() {
      if (confirm('Naozaj vymazať všetko?')) {
        postContent.value = '';
        document.getElementById('uploadedMedia').innerHTML = '';
        document.querySelectorAll('.platformBtn input').forEach(cb => cb.checked = false);
        document.querySelector('input[name="scheduleType"][value="now"]').checked = true;
        updatePreviews();
        updateMediaPreviews();
      }
    });

    // Hashtag functions
    window.addHashtag = function(hashtag) {
      postContent.value += ' ' + hashtag;
      postContent.focus();
      updatePreviews();
    };

    window.generateHashtags = function() {
      const suggestions = ['#portretnefilmy', '#videomarketing', '#businessvideo', '#korporatnevideo', '#brandstory'];
      const random = suggestions.slice(0, 3);
      postContent.value += '\n\n' + random.join(' ');
      updatePreviews();
      alert('Vygenerované AI hashtagy pridané!');
    };

    // Calendar functions
    window.prevMonth = function() {
      alert('Predošlý mesiac (funkcionalita v príprave)');
    };

    window.nextMonth = function() {
      alert('Ďalší mesiac (funkcionalita v príprave)');
    };
  </script>
</BaseLayout>

<style>
  /* Base styles */
  .heroSection {
    padding: 4rem 1.5rem 2rem;
    text-align: center;
    background: radial-gradient(ellipse at top, rgba(249,115,22,0.12) 0%, transparent 60%);
  }
  .heroSection h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }
  .heroLead {
    color: rgba(255,255,255,0.6);
  }

  .section {
    padding: 2rem 0 4rem;
  }

  /* Tabs */
  .adminTabs {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 2rem;
    border-bottom: 1px solid rgba(255,255,255,0.1);
    padding-bottom: 1rem;
    flex-wrap: wrap;
  }
  .adminTab {
    padding: 0.75rem 1.5rem;
    background: transparent;
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 8px;
    color: rgba(255,255,255,0.7);
    cursor: pointer;
    transition: all 0.2s;
    font-size: 0.95rem;
  }
  .adminTab:hover {
    background: rgba(255,255,255,0.05);
  }
  .adminTab.active {
    background: #f97316;
    border-color: #f97316;
    color: white;
  }
  .tabContent {
    display: none;
  }
  .tabContent.active {
    display: block;
  }

  .subTabs {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 2rem;
    flex-wrap: wrap;
  }
  .subTab {
    padding: 0.5rem 1rem;
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 6px;
    color: rgba(255,255,255,0.7);
    cursor: pointer;
    font-size: 0.9rem;
  }
  .subTab:hover {
    background: rgba(255,255,255,0.05);
  }
  .subTab.active {
    background: rgba(249,115,22,0.2);
    border-color: #f97316;
    color: #f97316;
  }
  .subTabContent {
    display: none;
  }
  .subTabContent.active {
    display: block;
  }

  /* Stats */
  .statsGrid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 1rem;
    margin-bottom: 2rem;
  }
  .statCard {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 12px;
    padding: 1.5rem;
    text-align: center;
    position: relative;
  }
  .statValue {
    display: block;
    font-size: 2rem;
    font-weight: 700;
    color: #f97316;
  }
  .statLabel {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.6);
  }
  .statChange {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    font-size: 0.75rem;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
  }
  .statChange.positive {
    background: rgba(34,197,94,0.2);
    color: #22c55e;
  }
  .statChange.negative {
    background: rgba(239,68,68,0.2);
    color: #ef4444;
  }

  /* Controls */
  .invoiceControls, .emailControls {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
  }
  .searchInput {
    flex: 1;
    min-width: 200px;
    padding: 0.75rem 1rem;
    background: rgba(0,0,0,0.3);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 8px;
    color: white;
    font-size: 1rem;
  }
  .searchInput:focus {
    outline: none;
    border-color: #f97316;
  }

  /* Table */
  .tableWrapper {
    overflow-x: auto;
  }
  .invoiceTable {
    width: 100%;
    border-collapse: collapse;
    min-width: 800px;
  }
  .invoiceTable th {
    background: rgba(255,255,255,0.05);
    padding: 1rem;
    text-align: left;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: rgba(255,255,255,0.6);
  }
  .invoiceTable td {
    padding: 1rem;
    border-bottom: 1px solid rgba(255,255,255,0.08);
    font-size: 0.95rem;
  }
  .invoiceTable tr:hover td {
    background: rgba(255,255,255,0.02);
  }
  .subtext {
    font-size: 0.8rem;
    color: rgba(255,255,255,0.5);
  }
  .amount {
    font-weight: 600;
    color: #22c55e;
  }
  .emptyState {
    text-align: center;
    color: rgba(255,255,255,0.5);
    padding: 3rem !important;
  }

  .badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
  }
  .badge--success {
    background: rgba(34,197,94,0.2);
    color: #22c55e;
  }

  /* Forms */
  .formCard {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 2rem;
    margin-bottom: 2rem;
  }
  .formGrid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1.25rem;
  }
  .field {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }
  .fieldFull {
    grid-column: 1 / -1;
  }
  .field label {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.7);
  }
  .field input,
  .field select,
  .field textarea {
    background: rgba(0,0,0,0.3);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 8px;
    padding: 0.75rem 1rem;
    color: #fff;
    font-size: 1rem;
    font-family: inherit;
  }
  .field input:focus,
  .field select:focus,
  .field textarea:focus {
    outline: none;
    border-color: #f97316;
  }
  .formActions {
    margin-top: 1.5rem;
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  #jsonCode {
    background: rgba(0,0,0,0.3);
    padding: 1rem;
    border-radius: 8px;
    overflow-x: auto;
    font-family: monospace;
    font-size: 0.9rem;
  }
  .help {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.5);
    margin-top: 0.5rem;
  }

  .stepsGrid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1.5rem;
  }
  .stepCard {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 12px;
    padding: 1.5rem;
    text-align: center;
  }
  .stepNum {
    display: inline-flex;
    width: 36px;
    height: 36px;
    background: #f97316;
    color: white;
    border-radius: 50%;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    margin-bottom: 0.75rem;
  }
  .stepCard h4 {
    margin-bottom: 0.25rem;
  }
  .stepCard p {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.6);
    margin: 0;
  }

  /* SOCIAL MEDIA STYLES */
  .composerLayout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
  }

  @media (max-width: 1024px) {
    .composerLayout {
      grid-template-columns: 1fr;
    }
  }

  .composerPanel, .previewPanel {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 2rem;
  }

  .platformSelector {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .platformBtn {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem;
    background: rgba(255,255,255,0.03);
    border: 2px solid rgba(255,255,255,0.08);
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s;
  }

  .platformBtn:hover {
    background: rgba(255,255,255,0.05);
  }

  .platformBtn input {
    display: none;
  }

  .platformBtn input:checked + .platformIcon {
    transform: scale(1.2);
  }

  .platformBtn.selected {
    border-color: #f97316;
    background: rgba(249,115,22,0.1);
  }

  .platformIcon {
    font-size: 2rem;
    transition: transform 0.2s;
  }

  .platformIcon.large {
    font-size: 3rem;
  }

  .textStats {
    text-align: right;
    font-size: 0.85rem;
    color: rgba(255,255,255,0.5);
    margin-top: 0.5rem;
  }

  .mediaUploadZone {
    border: 2px dashed rgba(255,255,255,0.15);
    border-radius: 12px;
    padding: 2rem;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
  }

  .mediaUploadZone:hover {
    border-color: #f97316;
    background: rgba(249,115,22,0.05);
  }

  .uploadIcon {
    font-size: 3rem;
    display: block;
    margin-bottom: 0.5rem;
  }

  .uploadedMedia {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
    gap: 0.75rem;
    margin-top: 1rem;
  }

  .mediaPreviewItem {
    position: relative;
    border-radius: 8px;
    overflow: hidden;
  }

  .mediaPreviewItem img {
    width: 100%;
    height: 100px;
    object-fit: cover;
  }

  .removeMedia {
    position: absolute;
    top: 0.25rem;
    right: 0.25rem;
    background: rgba(239,68,68,0.9);
    color: white;
    border: none;
    border-radius: 50%;
    width: 24px;
    height: 24px;
    cursor: pointer;
    font-size: 1.25rem;
    line-height: 1;
  }

  .hashtagSuggestions {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .hashtagTag {
    padding: 0.5rem 1rem;
    background: rgba(59,130,246,0.2);
    color: #3b82f6;
    border: none;
    border-radius: 20px;
    font-size: 0.85rem;
    cursor: pointer;
  }

  .schedulingPanel {
    background: rgba(255,255,255,0.03);
    padding: 1.5rem;
    border-radius: 12px;
    margin-top: 1.5rem;
  }

  .schedulingPanel .field {
    margin-bottom: 0.75rem;
  }

  .schedulingPanel label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .composerActions {
    display: flex;
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .previewTabs {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .previewTab {
    padding: 0.5rem 1rem;
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 6px;
    color: rgba(255,255,255,0.7);
    cursor: pointer;
    font-size: 0.85rem;
  }

  .previewTab.active {
    background: rgba(249,115,22,0.2);
    border-color: #f97316;
    color: #f97316;
  }

  .previewContent {
    display: none;
  }

  .previewContent.active {
    display: block;
  }

  .socialPreview {
    background: #fff;
    color: #333;
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 1rem;
  }

  .previewHeader {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .previewAvatar {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
  }

  .previewMeta {
    flex: 1;
  }

  .previewMeta strong {
    display: block;
    font-size: 0.95rem;
  }

  .previewTime {
    font-size: 0.8rem;
    color: #666;
  }

  .previewHandle {
    font-size: 0.85rem;
    color: #666;
  }

  .previewText {
    margin-bottom: 1rem;
    line-height: 1.5;
  }

  .previewMedia {
    margin-bottom: 1rem;
  }

  .previewStats {
    display: flex;
    gap: 2rem;
    padding-top: 0.75rem;
    border-top: 1px solid #eee;
    font-size: 0.9rem;
    color: #666;
  }

  .bestTimeCard {
    background: rgba(249,115,22,0.1);
    border: 1px solid rgba(249,115,22,0.2);
    border-radius: 12px;
    padding: 1.5rem;
    margin-top: 1rem;
  }

  .bestTimeCard h4 {
    margin-bottom: 0.75rem;
    color: #f97316;
  }

  .bestTimeCard p {
    margin: 0.25rem 0;
    font-size: 0.9rem;
  }

  /* Calendar */
  .calendarHeader {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
  }

  .calendarControls {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .calendar {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 2rem;
  }

  .calendarGrid {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 0.5rem;
  }

  .calendarDay {
    aspect-ratio: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding: 0.5rem;
    background: rgba(255,255,255,0.03);
    border-radius: 8px;
    font-size: 0.9rem;
  }

  .calendarDay.dayHeader {
    background: transparent;
    font-weight: 700;
    color: rgba(255,255,255,0.6);
  }

  .calendarDay.today {
    background: rgba(249,115,22,0.2);
    border: 2px solid #f97316;
  }

  .calendarPosts {
    margin-top: 0.5rem;
    width: 100%;
  }

  .calendarPost {
    font-size: 0.7rem;
    padding: 0.25rem;
    margin: 0.25rem 0;
    border-radius: 4px;
  }

  .calendarPost.fb {
    background: rgba(59,130,246,0.2);
  }

  .calendarPost.ig {
    background: rgba(236,72,153,0.2);
  }

  .scheduledPosts {
    margin-top: 2rem;
  }

  .postsList {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1rem;
  }

  .postItem {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 12px;
  }

  .postPlatforms {
    display: flex;
    gap: 0.25rem;
  }

  .platformBadge {
    font-size: 1.25rem;
  }

  .postContent {
    flex: 1;
    font-size: 0.9rem;
  }

  .postActions {
    display: flex;
    gap: 0.5rem;
  }

  .postStats {
    margin-top: 0.5rem;
    font-size: 0.85rem;
    color: rgba(255,255,255,0.5);
  }

  .dragHandle {
    cursor: move;
    color: rgba(255,255,255,0.3);
  }

  /* Queue */
  .queueSettings {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 2rem;
    margin-bottom: 2rem;
  }

  .queueList {
    margin-top: 2rem;
  }

  /* Analytics */
  .analyticsDateRange {
    margin-bottom: 2rem;
  }

  .chartPlaceholder {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 4rem 2rem;
    text-align: center;
    color: rgba(255,255,255,0.5);
    margin: 2rem 0;
  }

  .topPosts {
    margin-top: 2rem;
  }

  /* Media Library */
  .mediaFilters {
    display: flex;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .mediaGrid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 1rem;
  }

  .mediaItem {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 12px;
    overflow: hidden;
    cursor: pointer;
  }

  .mediaThumb {
    aspect-ratio: 1;
    background: rgba(255,255,255,0.1);
  }

  .mediaInfo {
    padding: 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .mediaName {
    font-size: 0.85rem;
    font-weight: 600;
  }

  .mediaDate {
    font-size: 0.75rem;
    color: rgba(255,255,255,0.5);
  }

  /* Accounts */
  .accountsList {
    display: grid;
    gap: 1.5rem;
  }

  .accountCard {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 2rem;
  }

  .accountCard.connected {
    border-color: rgba(34,197,94,0.3);
  }

  .accountHeader {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .accountInfo {
    flex: 1;
  }

  .accountInfo h4 {
    margin-bottom: 0.25rem;
  }

  .accountInfo p {
    font-size: 0.9rem;
    color: rgba(255,255,255,0.6);
    margin: 0;
  }

  .statusBadge {
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .statusBadge.active {
    background: rgba(34,197,94,0.2);
    color: #22c55e;
  }

  .statusBadge.inactive {
    background: rgba(107,114,128,0.2);
    color: #6b7280;
  }

  .accountStats {
    display: flex;
    gap: 2rem;
    padding: 1rem 0;
    border-top: 1px solid rgba(255,255,255,0.08);
    border-bottom: 1px solid rgba(255,255,255,0.08);
    margin-bottom: 1rem;
  }

  .accountActions {
    display: flex;
    gap: 0.75rem;
  }

  .btn--small {
    padding: 0.5rem 1rem;
    font-size: 0.85rem;
  }
</style>